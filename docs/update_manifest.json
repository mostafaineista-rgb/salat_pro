{
  "version": "1.0.1",
  "build": 2,
  "release_notes": "Salat Pro — match this build to pubspec version line (e.g. 1.0.1+2). Raise \"build\" when you ship an update. Android updates are delivered via Google Play; Windows can use a hosted installer URL if you add it."
}
